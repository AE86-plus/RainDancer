# RainDancer

This repository bundles two related open-source projects:

- `RainDancer/`
- `RainDancer-s/`

Each subdirectory contains its own README, training scripts, and test scripts.

## Notes

- Model weights are not stored in this GitHub repository.
- Please read the README inside each subproject before training or testing.
